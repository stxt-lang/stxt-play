# Dependencies
node_modules/

# web/ is NOT ignored: Cloudflare serves that directory straight from the repository,
# so web/css/ and web/js/ are build output that has to be committed.

# Logs
npm-debug.log*
*.log
